# SHARP Server dependencies
torch>=2.0.0
numpy>=1.24.0
Pillow>=9.0.0

# Core ML conversion (optional - for faster inference on Apple Silicon)
coremltools>=7.0

# ml-sharp dependencies (installed separately via pip install -e .)
# See: https://github.com/apple/ml-sharp
